# SHA-3 硬體設計兩人團隊分工與協作指南

本文件定義 SHA-3 硬體加速器的兩人開發分工、階段任務與介面注意事項。核心精神為**「基於介面合約的並行開發」**，確保雙方在整合時能無縫接軌。

---

## 總覽視角與 Phase 推進邏輯

| 開發階段 | 執行方式 | Person A (架構與控制端) | Person B (數學運算與海綿端) | 階段目標 |
| :--- | :--- | :--- | :--- | :--- |
| **Phase 1** | **必須同步** | `sha3_pkg.sv` | `sha3_pkg.sv` | 定義所有資料型別、狀態機 Enum 與常數。 |
| **Phase 2** | **獨立並行** | 狀態暫存器、省電控制 (循序邏輯) | Keccak 核心運算單元 (組合邏輯) | 完成核心運算與儲存。 |
| **Phase 3** | **獨立並行** | 主狀態機 FSM、微週期排程器 | Padding 處理、I/O 緩衝區 | 完成控制邏輯與資料格式化。 |
| **Phase 4** | **必須同步** | `sha3_ultra_low_power_top.sv` | `sha3_ultra_low_power_top.sv` | 頂層連線與系統模擬驗證。 |

> **💡 開發原則**：Phase 1 絕對要兩人一起討論並定案；Phase 2 與 Phase 3 兩人可依各自節奏獨立推進，不需互相等待；全部單元完成後，再一起進入 Phase 4 整合。

---

## 模組詳細分工與 I/O 介面規格

### Phase 1: 基礎建設 (Day 1)
這是所有檔案的基石，負責確立「黑盒子」的邊界與介面。

*   **共同撰寫：`sha3_pkg.sv`**
    *   **資料型別**：定義三維陣列型別 `typedef logic [63:0] state_t [0:4][0:4];`，簡化後續所有模組的 I/O 宣告。
    *   **常數宣告**：Keccak-f[1600] 規定的 24 個 Round Constants。
    *   **狀態定義**：FSM 所需的狀態 Enum (如 `IDLE`, `ABSORB`, `PROCESS`, `SQUEEZE`)。

### Phase 2: 核心攻堅

**👨‍💻 Person A 負責：時序與記憶體控制**
*   **`keccak_state_bank.sv` (狀態暫存器)**
    *   **I/O 介面**：`clk`, `rst_n`, `nxt_state`, `cur_state`, `wr_en`。
    *   **注意事項**：佔用面積最大的區塊 (1600 bits FF)。必須遵守 `wr_en == 1` 時才能在 Clock 正緣更新狀態，否則保持原值。
*   **`sha3_low_power_gating.sv` (可選/進階)**
    *   **I/O 介面**：`clk`, `sleep_en`, `gated_clk`。
    *   **注意事項**：需採用 Latch-based clock gating 以避免 Glitch。初期若不熟，可先略過，全域使用同一個 Clock。

**👨‍💻 Person B 負責：純組合邏輯運算**
*   **`keccak_theta_serial.sv` ~ `keccak_chi_row.sv` (Theta, Rho, Pi, Chi)**
    *   **I/O 介面**：`in_state`, `out_state`。
    *   **注意事項**：需特別注意陣列 Index (x, y, z) 以及 `(x+1) mod 5` 的邊界條件。無 Clock 與 Reset。
*   **`keccak_iota.sv` (Iota)**
    *   **I/O 介面**：`in_state`, `out_state`, `round_index`。
    *   **注意事項**：需根據傳入的 `round_index` 至 pkg 撈取對應的常數，與 `in_state[0][0]` 進行 XOR，其餘位置直接 Pass-through。

### Phase 3: 控制與外殼

**👨‍💻 Person A 負責：控制心臟與大腦**
*   **`keccak_round_scheduler.sv` (計數器)**
    *   **I/O 介面**：`start_rounds`, `round_index`, `round_done`。
    *   **注意事項**：收到 `start_rounds` 脈波後，開始從 0 數至 23；數滿時將 `round_done` 拉高 1 個 Clock 週期以通知主狀態機。
*   **`sha3_ctrl_fsm.sv` (主狀態機)**
    *   **I/O 介面**：接收 `in_valid`，發送 `out_valid`；控制內部的 `wr_en`, `start_rounds`, `absorb_en`。
    *   **注意事項**：整個設計的靈魂。負責決定何時吸收資料 (`IDLE` -> `ABSORB`)、何時啟動 24 回合運算 (`ABSORB` -> `PROCESS`)，以及何時輸出結果 (`PROCESS` -> `SQUEEZE`)。

**👨‍💻 Person B 負責：資料處理與格式化**
*   **`sha3_pad_domain.sv` (Padding 模組)**
    *   **I/O 介面**：`msg_in`, `msg_length`, `padded_block`。
    *   **注意事項**：負責實作 SHA-3 規定的 `10...01` 補齊規則。需精準計算 bit 的對齊與拼接。
*   **`sha3_rate_buffer.sv` (吸收緩衝區)**
    *   **注意事項**：負責將 Padded 完成的資料與當前 State 的 Rate 區塊進行 XOR (Sponge 結構的 Absorb 階段實作)。

### Phase 4: 系統整合 (共同作業)

*   **共同撰寫：`sha3_ultra_low_power_top.sv`**
    *   **任務**：本檔案為頂層設計，**內部不可包含任何運算邏輯**。
    *   **執行細節**：將 Phase 2 與 Phase 3 的所有子模組進行實例化 (Instantiate)，並將 Person A 的 FSM 控制訊號與 Person B 的資料路徑 (Data Path) 相互連接。
    *   **驗收**：只要嚴格遵守 Phase 1 訂下的介面合約，整合階段通常能迅速完成，隨後即可進入 Testbed 進行模擬驗證。


### 詳細撰寫順序與 I/O 訊號注意事項

以下按照推薦的開發順序，列出每個模組在傳入/傳出時必須特別注意的訊號。

#### Phase 1: 基礎建設 (雙人共同完成)

* **`sha3_pkg.sv`**
* **核心定義：** 定義三維陣列型別 `typedef logic [63:0] state_t [0:4][0:4];`，這樣後面的模組 I/O 就不用每次都寫一長串，直接宣告 `input state_t data_in` 即可。
* **常數定義：** 24 個 Round Constants (Keccak-f[1600] 規定) 的陣列。
* **狀態定義：** FSM 的 Enum，例如 `IDLE`, `ABSORB`, `PROCESS`, `SQUEEZE`。



---

#### Phase 2: 核心攻堅 (各自展開)

**【Person B：負責 Keccak 組合邏輯】**
這部分幾乎全是純組合邏輯 (`always_comb` 或 `assign`)，**沒有 Clock 和 Reset**。

* **`keccak_theta_serial.sv` 到 `keccak_chi_row.sv**`
* **I/O 注意：** 只需要 `input state_t in_state` 和 `output state_t out_state`。
* **注意陷阱：** 陣列的 Index 非常容易搞錯 (x, y, z)。一定要看清楚 SHA-3 規格書中 `(x+1) mod 5` 這種邊界條件。


* **`keccak_iota.sv`**
* **I/O 注意：** 除了 `in_state`，還需要 `input [4:0] round_index` (用來告訴這個模組現在是第幾回合)。
* **注意陷阱：** Iota 模組內部需要根據 `round_index` 去 `sha3_pkg.sv` 裡面撈對應的 Round Constant 與 `in_state[0][0]` 做 XOR，其他位置的資料則直接 Pass-through。



**【Person A：負責記憶體與省電控制】**
這部分與時序 (Sequential Logic) 息息相關。

* **`keccak_state_bank.sv` (狀態暫存器)**
* **I/O 注意：** 需要 `clk`, `rst_n`, `input state_t nxt_state`, `output state_t cur_state`，以及 `input wr_en` (寫入致能)。
* **注意陷阱：** 這是整個 SHA-3 最佔面積的地方 (1600 bits Flip-Flops)。只有當 FSM 給出 `wr_en == 1` 時，才能在 Clock 正緣更新狀態，否則必須保持原值。


* **`sha3_low_power_gating.sv` (可選/進階)**
* **I/O 注意：** `input clk`, `input sleep_en`, `output gated_clk`。
* **注意陷阱：** 要用 Latch-based clock gating 避免 Glitch。如果不熟，初期可以先略過這個模組，直接把 global clock 接給所有人。



---

#### Phase 3: 控制與外殼 (各自展開)

**【Person B：負責資料處理與外殼】**

* **`sha3_pad_domain.sv` (補 0 補 1 模組)**
* **I/O 注意：** `input [X:0] msg_in`, `input [X:0] msg_length`, `output [1087:0] padded_block` (以 SHA3-256 為例，Rate 為 1088 bits)。
* **注意陷阱：** SHA-3 規定結尾要補 `01`，然後中間全補 `0`，最後一個 bit 補 `1` (也就是 `10...01` 規則)。這裡要精準計算 bit 的對齊。


* **`sha3_rate_buffer.sv` (吸收緩衝區)**
* **I/O 注意：** 負責把 Padded 好的資料，和當前的 State 進行 XOR (這就是 Sponge 結構的 Absorb 階段)。



**【Person A：負責心臟與大腦】**

* **`keccak_round_scheduler.sv` (計數器)**
* **I/O 注意：** `input start_rounds`, `output [4:0] round_index`, `output round_done`。
* **注意陷阱：** 當收到 `start_rounds` 脈波，開始從 0 數到 23，數到 23 的時候把 `round_done` 拉高一個 Clock，通知 FSM 運算結束。


* **`sha3_ctrl_fsm.sv` (主狀態機)**
* **I/O 注意：** 接收外界的 `in_valid`，發送給外界的 `out_valid`。控制內部的 `wr_en`, `start_rounds`, `absorb_en`。
* **注意陷阱：** 這是最重要的靈魂。你必須決定何時把外部資料吃進來 (IDLE -> ABSORB)，何時啟動 24 回合運算 (ABSORB -> PROCESS)，以及運算完是繼續吃下一筆資料，還是吐出 Hash (PROCESS -> SQUEEZE)。



---

#### Phase 4: 系統整合

* **`sha3_ultra_low_power_top.sv`**
* 這是頂層檔案，裡面**不應該有任何運算邏輯**。
* 你們兩個人要做的事，就是把前面的模組全部 `instantiate` (實例化) 進來，然後像接水管一樣，把 Person A 的 FSM 控制訊號，接到 Person B 的資料路徑上。
* 只要前面的 I/O 都有對齊 `sha3_pkg.sv` 的規範，這裡通常半小時就能接完，接下來就是開心地跑 `01_run_vcs_rtl` 驗證成果了！