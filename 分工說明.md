這是一份為你們量身打造、完全符合 MD 檔案中 **「Candidate 3: 64-bit Lane-Serial Architecture (極低功耗折疊架構)」** 要求的全新分工指南。

我保持了原本的章節架構，但大幅升級了核心單元的 I/O 介面與時序控制（加入 `start`、`done` 握手訊號），並詳細標註了各個運算所需的**週期數 (Cycles)**。這樣不僅能實作多週期排程，還能完美達成「通道級時脈閘控 (Lane-level Clock Gating)」。

---

# SHA-3 硬體設計兩人團隊分工與協作指南 (Ultra-Low Power 版)

本文件定義 SHA-3 硬體加速器的兩人開發分工、階段任務與介面注意事項。核心精神為「基於介面合約的並行開發」，確保雙方在整合時能無縫接軌。為達到極低功耗要求，本專案全面採用 **多週期序列化 (Multi-Cycle Serial)** 與 **握手協定 (Handshake)** 進行設計。

### 總覽視角與 Phase 推進邏輯

| 開發階段 | 執行方式 | Person A (架構與全域控制端) | Person B (序列運算與海綿端) | 階段目標 |
| --- | --- | --- | --- | --- |
| **Phase 1** | 必須同步 | `sha3_pkg.sv` | `sha3_pkg.sv` | 定義所有資料型別、狀態機 Enum 與常數。 |
| **Phase 2** | 獨立並行 | 通道級狀態暫存器、省電控制 | Keccak 序列化運算單元 (具獨立時序) | 完成**多週期**核心運算與折疊儲存。 |
| **Phase 3** | 獨立並行 | 主狀態機、微週期握手排程器 | Padding 處理、I/O 緩衝區 | 完成交握控制邏輯與資料格式化。 |
| **Phase 4** | 必須同步 | `sha3_ultra_low_power_top.sv` | `sha3_ultra_low_power_top.sv` | 頂層連線與系統模擬驗證。 |
| **Phase 5** | 可延後、不阻塞 Phase 4 | `sha3_perf_counter.sv` | — | Phase 4 功能驗證(雜湊值比對)通過後，補上功耗/效能量測計數器。 |

> 💡 **開發原則：** Phase 1 絕對要兩人一起討論並定案；Phase 2 與 Phase 3 兩人可依各自節奏獨立推進，請嚴格遵守 `start` 與 `done` 的交握合約；全部單元完成後，再一起進入 Phase 4 整合。
>
> ⚠️ **補充說明：** 原分工遺漏了 `sha3_output_formatter.sv`(Squeeze 輸出格式化)。這個模組**會擋住 Phase 4 的雜湊值驗證**(沒有它就無法把運算完的 state 轉成正確輸出比對 golden model),因此併入 Phase 3、指派給 Person B,與 `sha3_rate_buffer.sv` 同屬「資料格式化」性質。`sha3_perf_counter.sv` 則只影響功耗量測、不影響雜湊正確性,獨立列為 Phase 5,待 Phase 4 功能驗證通過後再做,指派給 Person A 以平衡兩人模組數量。

---

### 模組詳細分工與 I/O 介面規格

#### Phase 1: 基礎建設 (Day 1)

這是所有檔案的基石，負責確立「黑盒子」的邊界與介面。

* **共同撰寫：`sha3_pkg.sv**`
* **資料型別：** 定義三維陣列型別 `typedef logic [63:0] state_t [0:4][0:4];`。
* **常數宣告：** Keccak-f[1600] 規定的 24 個 Round Constants。
* **狀態定義：** FSM 所需的狀態 Enum (如 `IDLE`, `ABSORB`, `PROCESS`, `SQUEEZE`)，以及 Theta/Chi 內部的微狀態 Enum。



#### Phase 2: 核心攻堅

👨‍💻 **Person A 負責：時序與記憶體控制**

* **`keccak_state_bank.sv` (通道級狀態暫存器)**
* **I/O 介面：** `clk`, `rst_n`, `nxt_state`, `cur_state`, `[24:0] lane_wr_en`。
* **注意事項：** 為了極限省電，寫入致能 `lane_wr_en` 必須擴展為 25 bits。只有對應 bit 為 1 的 64-bit Lane 才能在 Clock 正緣更新，未啟用的 Lane 必須完美靜止以節省動態功耗。


* **`sha3_low_power_gating.sv` (必須實作)**
* **I/O 介面：** `clk`, `[24:0] sleep_en`, `[24:0] gated_clk`。
* **注意事項：** 配合狀態暫存器，採用 Latch-based clock gating 產生 25 組獨立時脈，徹底斬斷無效的 Clock Tree 耗電。



👨‍💻 **Person B 負責：多週期序列化邏輯運算 (Lane/Row Serial)**

* **`keccak_theta_serial.sv` (Theta 序列化模組)**
* **I/O 介面：** `clk`, `rst_n`, `start`, `in_state`, `out_state`, `done`。
* **週期說明 (共約 11 Cycles)：**
* Cycle 1~5：循序計算 5 個 Column 的 parity $C[x]$ 並存入內部快取。
* Cycle 6：計算 $D[x]$。
* Cycle 7~11：逐列 (Column-by-column) 更新 `out_state`。


* **注意事項：** 收到 `start` 脈波才開始動作，運算完畢拉高 `done` 1 個週期。這是消除 XOR Tree Glitch 的關鍵。


* **`keccak_rho_pi_wire.sv` (Rho & Pi 純連線模組)**
* **I/O 介面：** `in_state`, `out_state`。
* **週期說明 (0 Cycle)：** 維持純組合邏輯，利用 fixed wiring 達成，不消耗任何週期。


* **`keccak_chi_row.sv` (Chi 列序列化模組)**
* **I/O 介面：** `clk`, `rst_n`, `start`, `in_state`, `out_state`, `done`。
* **週期說明 (共 5 Cycles)：** 每次只取 5 個 Lanes (1 個 Row, 320 bits) 進行非線性 AND/XOR 運算，分 5 次完成整塊 State。
* **注意事項：** 同樣依賴 `start` 與 `done` 進行握手。


* **`keccak_iota.sv` (Iota)**
* **I/O 介面：** `in_state`, `out_state`, `[4:0] round_index`。
* **週期說明 (0 Cycle)：** 純組合邏輯。將 `round_index` 對應的常數與 `in_state[0][0]` 進行 XOR。



#### Phase 3: 控制與外殼

👨‍💻 **Person A 負責：控制心臟與交握大腦**

* **`keccak_round_scheduler.sv` (微週期交握排程器)**
* **I/O 介面：** `start_process`, `theta_done`, `chi_done`, `theta_start`, `chi_start`, `[4:0] round_index`, `process_done`。
* **注意事項：** 這是連接 Person B 多週期模組的橋樑。負責發出 `theta_start`，等待 `theta_done`，再經過 Rho/Pi，接著發出 `chi_start`，等待 `chi_done`。這稱為「1 個 Round」。數滿 24 個 Rounds 後，將 `process_done` 傳給主狀態機。


* **`sha3_ctrl_fsm.sv` (主狀態機)**
* **I/O 介面：** `in_valid`, `out_valid`, `start_process`, `process_done`, `absorb_en`。
* **注意事項：** 全域大腦。決定何時將外部資料與 State XOR (`ABSORB`)，何時觸發 24 回合排程器 (`PROCESS`)，以及何時輸出 (`SQUEEZE`)。



👨‍💻 **Person B 負責：資料處理與格式化**

* **`sha3_pad_domain.sv` (Padding 模組)**
* **I/O 介面：** `msg_in`, `msg_length`, `padded_block`。
* **注意事項：** 實作 SHA-3 的 **10...01** 補齊規則 (無時序，純組合邏輯的對齊與拼接)。


* **`sha3_rate_buffer.sv` (吸收緩衝區)**
* **注意事項：** 配合主 FSM 的 `absorb_en`，將 Padded 資料與 State 的 Rate 區段進行 XOR。


* **`sha3_output_formatter.sv` (Squeeze 輸出格式化模組，新補分工)**
* **I/O 介面：** `clk`, `rst_n`, `start`, `in_state`, `[31:0] out_data`, `out_valid`, `done`。
* **週期說明 (共 8 Cycles，對應 SHA3-256 的 256-bit 輸出)：**
* 依 Keccak lane 線性化順序 (z = x + 5y)，SHA3-256 輸出只需取 `y=0` 這一整列的前 4 個 Lane (x=0~3)。
* Cycle 1~2：輸出 Lane(0,0) 的低 32 bits / 高 32 bits。
* Cycle 3~4：輸出 Lane(1,0)。
* Cycle 5~6：輸出 Lane(2,0)。
* Cycle 7~8：輸出 Lane(3,0)，並在最後一個 cycle 拉高 `done`。
* **注意事項：** 純資料多工/移位，不涉及運算，內部用 `squeeze_state_e` (SQZ_IDLE/SQZ_LO/SQZ_HI/SQZ_DONE，已加入 `sha3_pkg.sv`) 搭配 Lane counter 選取當前 Lane 與高低 32-bit half。務必收到 `start` 才動作、`out_valid` 只在資料有效的 cycle 拉高，避免對未啟用區段做無謂切換（與 Person A 的 operand isolation 精神一致）。這是 Phase 4 能否正確跑出雜湊值的關鍵模組，原分工遺漏此模組，請優先完成。



#### Phase 4: 系統整合 (共同作業)

* **共同撰寫：`sha3_ultra_low_power_top.sv**`
* **任務：** 頂層設計，內部不可包含任何運算邏輯。
* **執行細節：** 實例化所有子模組。將 Person A 的 FSM 與排程器訊號 (如 `start`、`done`、`[24:0] lane_wr_en`)，精準對接到 Person B 的序列化運算單元與 Person A 的狀態暫存器上；`sha3_ctrl_fsm` 進入 `ST_SQUEEZE` 時觸發 `sha3_output_formatter` 的 `start`，其 `out_data`/`out_valid` 接到 Output FIFO（或直接對外輸出）。
* **驗收：** I/O 徹底對齊後，直接進入 Testbed，用 Python `hashlib.sha3_256` 當 golden model 比對雜湊值是否正確。
* **保留給 Phase 5 的接口：** 整合時請預留 `sha3_perf_counter.sv` 之後要接的觀測訊號位置（`start_process`、`process_done`、`[24:0] lane_wr_en`、`[4:0] round_index`），現階段不用實作，只要確保這些訊號在 top 層可以被存取即可，避免 Phase 5 補模組時要動到既有接線。



---

### 詳細撰寫順序與 I/O 訊號注意事項

以下按照推薦的開發順序，列出每個模組在傳入/傳出時必須特別注意的陷阱。

#### Phase 1: 基礎建設 (雙人共同完成)

* **`sha3_pkg.sv`**
* **核心定義：** `typedef logic [63:0] state_t [0:4][0:4];`。
* **常數定義：** 24 個 Round Constants。
* **擴充狀態定義：** 新增 Theta 專用的 Enum (如 `THETA_IDLE`, `THETA_C`, `THETA_D`, `THETA_UPDATE`) 與 Chi 專用的 Enum。



#### Phase 2: 核心攻堅 (各自展開)

**【Person B：負責 Keccak 多週期序列邏輯】** 這是本次架構改版的核心，務必加上時序控制！

* **`keccak_theta_serial.sv`**
* **I/O 注意：** `input clk`, `input rst_n`, `input start`, `input state_t in_state`, `output state_t out_state`, `output logic done`。
* **注意陷阱：** 內部需要實作一個小型計數器或狀態機。在 Cycle 1~5 時，把算好的 $C[x]$ 存進內部的 DFF 快取中；切勿在算完前就把 `done` 拉高。


* **`keccak_chi_row.sv`**
* **I/O 注意：** 介面同上 (`clk`, `rst_n`, `start`, `done`)。
* **注意陷阱：** 一次只抓取 `in_state[0~4][y]` (共 320 bits) 進行運算，利用內部 Counter `y` 從 0 數到 4。第 5 個 Cycle 結束時拉高 `done`。



**【Person A：負責記憶體與通道級省電】**

* **`keccak_state_bank.sv` (狀態暫存器)**
* **I/O 注意：** `input [24:0] lane_wr_en` 是成敗關鍵。
* **注意陷阱：** 1600 bits 必須拆成 25 塊 64-bit 的 Always block (或 generate block)。只有當 `lane_wr_en[i] == 1` 時，第 `i` 個 Lane 才更新。這需要你與排程器高度連動。



#### Phase 3: 控制與外殼 (各自展開)

**【Person A：負責心臟與大腦】**

* **`keccak_round_scheduler.sv` (排程器)**
* **I/O 注意：** 需要接收來自 Theta 和 Chi 的 `done` 訊號。
* **注意陷阱：** 這是個微狀態機。收到 FSM 的 `start_process` 後，流程為：觸發 `theta_start` -> 等 `theta_done` -> 觸發 `chi_start` -> 等 `chi_done` -> `round_index + 1`。重複 24 次後發出 `process_done`。



**【Person B：負責資料處理與外殼】**

* **`sha3_pad_domain.sv`**
* **I/O 注意：** `input [X:0] msg_in`, `input [X:0] msg_length`, `output [1087:0] padded_block`。
* **注意陷阱：** 維持純組合邏輯，精準計算 `10...01` 對齊即可。


* **`sha3_output_formatter.sv` (新補分工)**
* **I/O 注意：** `input clk`, `input rst_n`, `input start`, `input state_t in_state`, `output logic [31:0] out_data`, `output logic out_valid`, `output logic done`。
* **注意陷阱：** 容易搞錯 Lane 的線性化順序，記得是 `z = x + 5y`，SHA3-256 只需要 `y=0` 那一列的 `x=0~3`，不是隨便挑 4 個 Lane。另外要注意 64-bit Lane 拆成兩個 32-bit word 時的 endianness，務必和 Python `hashlib.sha3_256` 的 byte order 對齊，否則 Phase 4 比對雜湊值會一直失敗卻抓不到原因。



#### Phase 4: 系統整合

* **`sha3_ultra_low_power_top.sv`**
* 無邏輯，純粹連線。特別注意 `start` 和 `done` 的交握網路是否有正確串接，以及 `[24:0] lane_wr_en` 是否有根據當前 Theta/Chi 算到的進度正確解碼並派發給 `state_bank`。

---

#### Phase 5: 效能量測強化 (Phase 4 通過後再做，新補分工)

**【Person A：負責 `sha3_perf_counter.sv`】**

* **I/O 注意：** `input clk`, `input rst_n`, `input counter_en`, `input start_process`, `input process_done`, `input [24:0] lane_wr_en`, `input [4:0] round_index`, `output logic [31:0] cycle_count`, `output logic [15:0] perm_count`, `output logic [31:0] lane_toggle_count`。
* **注意陷阱：** 這個模組只能「聽」訊號、絕對不能「動」訊號——不可以回寫 state，也不可以影響 FSM 或排程器的任何判斷邏輯，純粹是外掛的觀測電路。`cycle_count` 在 `start_process` 拉高時歸零開始計數，`process_done` 拉高時鎖存目前值；`lane_toggle_count` 每個 cycle 對 `lane_wr_en` 做 popcount 累加，這是用來證明「通道級時脈閘控」真的有壓低切換活動的量化證據，之後寫報告或投稿時會用到（呼應 sha3LP.md 的 cycles_per_block、active_lane_banks_per_round 指標）。因為不影響雜湊功能正確性，等 Phase 4 用 golden model 比對雜湊值全部通過後再做即可，不會卡到主線進度。