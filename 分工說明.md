# SHA-3 硬體設計兩人團隊分工與協作指南 (Ultra-Low Power 版)

本文件對應 **「Candidate 3: 64-bit Lane-Serial Architecture (極低功耗折疊架構)」**（見 `ultra_low_power_sha3_architecture.md`），定義 SHA-3 硬體加速器的兩人開發分工、階段任務與介面注意事項。核心精神為「基於介面合約的並行開發」，確保雙方在整合時能無縫接軌。為達到極低功耗要求，本專案全面採用 **多週期序列化 (Multi-Cycle Serial)** 與 **握手協定 (Handshake)** 進行設計，並以 **Lane 級時脈閘控 (Lane-level Clock Gating)** 取代任何形式的顯式 write-enable 訊號。

> 本版本為對齊「現行程式碼」的修訂版：移除已經不存在的 `lane_wr_en` 訊號、補上原本漏列的 `lane_active` 埠、更新各模組實際週期數與內部機制說明，並取消 Phase 5（效能計數器），Phase 4 驗證通過後直接進入 Testbed 收尾。

### 總覽視角與 Phase 推進邏輯

| 開發階段 | 執行方式 | Person A (架構與全域控制端) | Person B (序列運算與海綿端) | 階段目標 |
| --- | --- | --- | --- | --- |
| **Phase 1** | 必須同步 | `sha3_pkg.sv` | `sha3_pkg.sv` | 定義所有資料型別、狀態機 Enum 與常數。 |
| **Phase 2** | 獨立並行 | 通道級狀態暫存器、省電控制 | Keccak 序列化運算單元 (具獨立時序) | 完成**多週期**核心運算與折疊儲存。 |
| **Phase 3** | 獨立並行 | 主狀態機、微週期握手排程器 | Padding 處理、I/O 緩衝區 | 完成交握控制邏輯與資料格式化。 |
| **Phase 4** | 必須同步 | `sha3_ultra_low_power_top.sv` | `sha3_ultra_low_power_top.sv` | 頂層連線、系統模擬驗證，**驗證通過即結案，不再進入 Phase 5**。 |

> 💡 **開發原則：** Phase 1 絕對要兩人一起討論並定案；Phase 2 與 Phase 3 兩人可依各自節奏獨立推進，請嚴格遵守 `start` 與 `done` 的交握合約；全部單元完成後，再一起進入 Phase 4 整合，並直接接上既有的 `test/TESTBED.sv` + `test/PATTERN.sv` 做雜湊值驗證。
>
> ⚠️ **補充說明：** 原分工遺漏了 `sha3_output_formatter.sv`(Squeeze 輸出格式化)。這個模組**會擋住 Phase 4 的雜湊值驗證**(沒有它就無法把運算完的 state 轉成正確輸出比對 golden model),因此併入 Phase 3、指派給 Person B,與 `sha3_rate_buffer.sv` 同屬「資料格式化」性質。
>
> ⚠️ **本專案不做效能/功耗計數器：** 原本規劃的 `sha3_perf_counter.sv`（Phase 5）已取消，不列入分工。Phase 4 golden model 比對通過後，直接視為專案完成，不再保留給後續模組的觀測介面。

---

### 模組詳細分工與 I/O 介面規格

#### Phase 1: 基礎建設 (Day 1)

這是所有檔案的基石，負責確立「黑盒子」的邊界與介面。

* **共同撰寫：`sha3_pkg.sv`**
* **資料型別：** 定義三維陣列型別 `typedef logic [63:0] state_t [0:4][0:4];`（unpacked array，`[x][y]` 索引）。另有 `row_t`/`col_t`（`logic [63:0] [0:4]`）供未來擴充，目前實際模組介面沒有用到。
* **常數宣告：** Keccak-f[1600] 規定的 24 個 Round Constants（`keccak_rc()` function）。
* **狀態定義：**
  * 主 FSM：`ST_IDLE`, `ST_PAD`, `ST_ABSORB`, `ST_RUN_ROUND`, `ST_SQUEEZE`, `ST_DONE`。
  * Theta 微狀態：`theta_state_e` = `THETA_IDLE` / `THETA_CALC_C` / `THETA_CALC_D` / `THETA_UPDATE`。
  * Chi 微狀態：`chi_state_e` = `CHI_IDLE` / `CHI_CALC`。
  * Round Scheduler 微狀態：`round_sched_state_e` = `SCHED_IDLE` / `SCHED_THETA_START` / `SCHED_THETA_WAIT` / `SCHED_RHO_PI_START` / `SCHED_RHO_PI_WAIT` / `SCHED_CHI_START` / `SCHED_CHI_WAIT` / `SCHED_DONE`。
  * 輸出格式化微狀態：`sqz_state_e` = `SQZ_IDLE` / `SQZ_RUN` / `SQZ_DONE`（用 `cnt[0]` 分辨低/高 32-bit，不是兩個獨立狀態）。
* **主 FSM 固定流程：** `ST_IDLE -> ST_PAD -> ST_ABSORB -> ST_RUN_ROUND -> ST_SQUEEZE -> ST_DONE -> ST_IDLE`。其中主 FSM 只管理一筆 SHA-3 工作的高階流程，24-round 的內部細節由 `keccak_round_scheduler.sv` 隱藏並負責。

> ⚠️ **已知問題（待修正，非本次文件範圍）：** 目前 `keccak_rc()` 裡 `round_index=18` 與 `round_index=23` 兩個常數各多了一個 `0x0080` bit（應為 `64'h000000000000800A` 與 `64'h8000000080008008`），會讓 round 18 之後的雜湊結果錯誤。修正只需要改這兩行，之後再重新用 empty message 跑一次 golden model 比對即可。



#### Phase 2: 核心攻堅

👨‍💻 **Person A 負責：時序與記憶體控制**

* **`keccak_state_bank.sv` (通道級狀態暫存器)**
* **I/O 介面：** `input [24:0] lane_clk`, `input rst_n`, `input state_t nxt_state`, `output state_t cur_state`。
* **注意事項：** 沒有獨立的 `lane_wr_en` 埠——「要不要寫入」完全由 `lane_clk[i]` 有沒有出現有效 posedge 決定（純 clock gating，不是「clock 一直跳、用 enable 擋 D-input」）。25 個 lane 各自是一個 `generate` 出來的 `always_ff @(posedge lane_clk[LANE_IDX] or negedge rst_n)`，`LANE_IDX = y*COL_NUM + x`。目前模組內建了 `debug_cur_state_0_0` / `debug_nxt_state_0_0`（把 `cur_state[0][0]` / `nxt_state[0][0]` 接成 1D wire）方便波形工具直接觀察 lane(0,0)，除錯時可以直接拉這兩條線搭配 `lane_clk[0]` 看。


* **`sha3_low_power_gating.sv` (必須實作)**
* **I/O 介面：** `clk`, `[24:0] sleep_en`, `[24:0] gated_clk`。
* **注意事項：** `sleep_en` 採 active-high 定義 (`1` 表示停止對應 Lane 的時脈)。因為 `lane_wr_en` 已經從 `keccak_state_bank.sv` 移除，`sleep_en`（進而 `gated_clk`）就是全系統唯一決定「哪個 lane 這個 cycle 可以被寫入」的訊號，沒有第二條路徑。採用 Latch-based clock gating（`always_latch`，只在 `clk` 為低的半週期透明、`clk` 為高時鎖住)產生 25 組獨立時脈，並將 `gated_clk` 對接至 State Bank 的 `lane_clk`，徹底斬斷無效的 Clock Tree 耗電。



👨‍💻 **Person B 負責：多週期序列化邏輯運算 (Lane/Row Serial)**

* **`keccak_theta_serial.sv` (Theta 序列化模組)**
* **I/O 介面：** `clk`, `rst_n`, `start`, `in_state`, `out_state`, `done`, `[24:0] lane_active`（原分工漏列，`lane_active` 是 Top 層 `state_active_mask` 多工鏈的必要輸入，務必實作）。
* **週期說明 (共 11 Cycles)：**
* Cycle 1~5 (`THETA_CALC_C`)：循序計算 5 個 Column 的 parity $C[x]$ 並存入內部快取。
* Cycle 6 (`THETA_CALC_D`)：計算 $D[x]$。
* Cycle 7~11 (`THETA_UPDATE`)：逐欄 (column-by-column) 更新 `out_state`，每個 cycle 只讓該欄 5 個 lane 的 `lane_active` 拉高，最後一個 cycle 拉高 `done`。
* **注意事項：** 收到 `start` 脈波才開始動作，運算完畢拉高 `done` 1 個週期。目前實作是「0 register 直讀」：`THETA_UPDATE` 階段直接用 `out_state[cnt][y] = in_state[cnt][y] ^ D[cnt]`，不再另外鎖存一份 `base_state`。這是安全的，因為每個 cycle 只碰當前 `cnt` 那一欄、其他欄這一輪還沒被寫過，所以 `in_state` 讀到的值一定是本輪 Theta 開始前的原始值；但如果之後要改動排程（例如允許中途插隊、或欄與欄之間不再嚴格互斥），這個假設就會被打破，要小心。


* **`keccak_rho_pi_wire.sv` (Rho & Pi 純連線模組)**
* **I/O 介面：** `in_state`, `out_state`。
* **週期說明 (0 Cycle)：** 純組合邏輯。對每個 `(x,y)` 用固定的 `pi_source_x()` / `rho_offset()` 決定來源座標與旋轉量，因為迴圈邊界是常數 (0~4)，工具會把它展開成 25 條固定接線，不是可變寬度的 barrel shifter，符合設計指南「用 wiring、不要用 barrel shifter」的要求。


* **`keccak_chi_row.sv` (Chi 列序列化模組)**
* **I/O 介面：** `clk`, `rst_n`, `start`, `in_state`, `out_state`, `done`, `[24:0] lane_active`（原分工漏列）。
* **週期說明 (共 5 Cycles)：** 每次只取 5 個 Lanes (1 個 Row, 320 bits) 進行非線性 AND/XOR 運算，分 5 次 (`cnt = 0~4`) 完成整塊 State。
* **注意事項：** 內部有一個 `frozen_state` 暫存器，只在 `start` 拉高的那個 cycle 鎖存一次 `in_state`（也就是 Rho/Pi 排列完的整包 state），之後 5 個 cycle 的 χ 運算全部讀 `frozen_state`、不讀即時的 `in_state`。**這個 snapshot 是必要的、不是隨手加的保險**：因為 Pi 會把資料跨 row 搬動，第 `cnt` row 的 χ 運算其實需要「原本分散在其他 row」的資料，如果不凍結，等 row 0 已經把自己的輸出寫回 `cur_state`、Rho/Pi 又是純組合邏輯即時反映最新 `cur_state`，後面幾個 row 就會讀到「已經被 χ 動過」的髒資料。務必保留這個機制。



#### Phase 3: 控制與外殼

👨‍💻 **Person A 負責：控制心臟與交握大腦**

* **`keccak_round_scheduler.sv` (微週期交握排程器)**
* **I/O 介面：** `clk`, `rst_n`, `start_process`, `theta_done`, `chi_done`, `theta_start`, `chi_start`, `[4:0] round_index`, `process_done`。
* **注意事項：** 這是連接 Person B 多週期模組的橋樑，內部是 8 態微狀態機：`SCHED_IDLE -> SCHED_THETA_START -> SCHED_THETA_WAIT -> SCHED_RHO_PI_START -> SCHED_RHO_PI_WAIT -> SCHED_CHI_START -> SCHED_CHI_WAIT -> (回到 SCHED_THETA_START 或前進到 SCHED_DONE)`。Rho/Pi 本身是 0-cycle 組合邏輯，但排程器仍花 2 個 cycle (`RHO_PI_START`/`RHO_PI_WAIT`) 純粹做交握轉場，這是目前設計「每輪 20 cycle」裡唯二沒有實際運算、單純是握手開銷的 cycle，如果要進一步壓 cycle 數，這裡是第一個可以優化的地方。實測每輪固定為 **20 cycles**（`THETA_START`1 + `THETA_WAIT`11 + `RHO_PI_START`1 + `RHO_PI_WAIT`1 + `CHI_START`1 + `CHI_WAIT`5），24 輪合計 **480 cycles** 完成一次 Keccak-f[1600] permutation，數滿 24 個 Rounds 後，將 `process_done` 傳給主狀態機。


* **`sha3_ctrl_fsm.sv` (主狀態機)**
* **I/O 介面：** `clk`, `rst_n`, `in_valid`, `process_done`, `squeeze_done`, `absorb_en`, `start_process`, `squeeze_start`, `hash_done`。
* **注意事項：** 全域大腦。決定何時將外部資料與 State XOR (`ST_ABSORB`)、何時以單週期 `start_process` 觸發 24 回合排程器 (`ST_RUN_ROUND`)，以及何時以單週期 `squeeze_start` 觸發輸出格式化 (`ST_SQUEEZE`)。在 `ST_RUN_ROUND` 等待 `process_done`，在 `ST_SQUEEZE` 等待 `squeeze_done`，最後以 `ST_DONE` 產生單週期 `hash_done`。
* **`out_valid` 歸屬：** `out_valid` 由 `sha3_output_formatter.sv` 根據實際 32-bit 輸出節拍產生，**不由主 FSM 產生**，避免兩個模組同時驅動或輸出有效時間與資料不同步。
* **輸入資料保存：** Top 層必須在 `ST_IDLE` 接收到 `in_valid` 時鎖存 `msg_in` 與 `msg_length`，讓 Padding 與 Absorb 階段使用穩定資料；不可要求外部在整段運算期間持續保持輸入。



👨‍💻 **Person B 負責：資料處理與格式化**

* **`sha3_pad_domain.sv` (Padding 模組)**
* **I/O 介面：** `msg_in`, `msg_length`, `padded_block`。
* **注意事項：** 實作 SHA-3 的 **10...01** 補齊規則 (無時序，純組合邏輯的對齊與拼接)。


* **`sha3_rate_buffer.sv` (吸收緩衝區)**
* **I/O 介面：** `in_state`, `padded_block`, `absorb_en`, `out_state`。
* **注意事項：** 配合主 FSM 的 `absorb_en`，將 Padded 資料與 State 的 Rate 區段（17 個 lane）進行 XOR。


* **`sha3_output_formatter.sv` (Squeeze 輸出格式化模組)**
* **I/O 介面：** `clk`, `rst_n`, `start`, `in_state`, `[31:0] out_data`, `out_valid`, `done`。
* **週期說明 (共 8 Cycles，對應 SHA3-256 的 256-bit 輸出)：**
* 依 Keccak lane 線性化順序 (z = x + 5y)，SHA3-256 輸出只需取 `y=0` 這一整列的前 4 個 Lane (x=0~3)。
* Cycle 1~2：輸出 Lane(0,0) 的低 32 bits / 高 32 bits。
* Cycle 3~4：輸出 Lane(1,0)。
* Cycle 5~6：輸出 Lane(2,0)。
* Cycle 7~8：輸出 Lane(3,0)，並在最後一個 cycle 拉高 `done`。
* **注意事項：** 純資料多工/移位，不涉及運算，內部用 `sqz_state_e` (`SQZ_IDLE`/`SQZ_RUN`/`SQZ_DONE`) 搭配 3-bit `cnt`（`cnt[2:1]` 選 Lane 編號、`cnt[0]` 選低/高 32-bit）依序輸出，不是分開的 SQZ_LO/SQZ_HI 狀態。務必收到 `start` 才動作、`out_valid` 只在資料有效的 cycle 拉高，避免對未啟用區段做無謂切換（與 Person A 的 operand isolation 精神一致）。這是 Phase 4 能否正確跑出雜湊值的關鍵模組。



#### Phase 4: 系統整合 (共同作業)

* **共同撰寫：`sha3_ultra_low_power_top.sv`**
* **任務：** 頂層整合。內部只允許子模組實例化、訊號接線，以及在 `in_valid` 被接受時保存 `msg_in`/`msg_length` 的輸入暫存器；不可在 Top 內撰寫 Padding、Theta、Rho/Pi、Chi、Iota 等 SHA-3 運算邏輯。
* **執行細節：**
  * 實例化所有子模組。Top 層在 `in_valid` 被主 FSM 接受時鎖存 `msg_in`/`msg_length`；將主 FSM 的 `start_process` 與排程器的 `process_done` 串成交握，並將排程器的 `theta_start`/`chi_start` 對接至對應運算模組。
  * `keccak_iota.sv` 的 `in_state` 接的是 `chi_state`（Chi 模組即時的組合輸出），不是 `cur_state`。
  * Top 層用一個 `always_comb` 依優先序決定 `nxt_state` 與 `state_active_mask`：`accept_msg`(整包清零，`ALL_LANES`) → `absorb_en`(寫入 `absorbed_state`，`RATE_LANES`) → `theta_lane_active`(寫入 `theta_state`) → `chi_lane_active`(寫入 `chi_state`，**但 lane(0,0) 這一格改寫 `iota_state`**) → 預設值 (`nxt_state = cur_state`，`state_active_mask = 0`，保持不動)。也就是說 Iota 的 round-constant XOR 是**在 Chi 算 row 0 的那個 cycle 一起完成**，不再等 `chi_done` 之後另外多花一個 cycle 單獨寫 lane(0,0)；`round_index` 在這個時間點還沒被排程器遞增，所以 XOR 用的常數仍然是本輪正確的值。
  * 主 FSM 進入 `ST_SQUEEZE` 時以單週期 `squeeze_start` 觸發 `sha3_output_formatter` 的 `start`，再以 Formatter 的 `done` 回接主 FSM 的 `squeeze_done`。Formatter 的 `out_data`/`out_valid` 直接接到外部輸出，不可由主 FSM 重複產生 `out_valid`。
  * `sleep_en = ~state_active_mask`，接到 `sha3_low_power_gating`，其 `gated_clk` 一對一接到 `keccak_state_bank` 的 `lane_clk`。**沒有 `lane_wr_en` 這個訊號**，寫入與否完全由這條 clock gating 路徑決定，Person A 在 Top 層要檢查的是 `state_active_mask` 有沒有正確反映「這個 cycle 誰在動」，而不是去找一個獨立的 enable bus。
* **驗收：** I/O 徹底對齊後，直接接上 `test/TESTBED.sv` + `test/PATTERN.sv`，用 Python `hashlib.sha3_256` 當 golden model 比對雜湊值是否正確。**這是本專案的最後一步**，通過即完成，不需要再開新的 Phase。

---

### 詳細撰寫順序與 I/O 訊號注意事項

以下按照推薦的開發順序，列出每個模組在傳入/傳出時必須特別注意的陷阱。

#### Phase 1: 基礎建設 (雙人共同完成)

* **`sha3_pkg.sv`**
* **核心定義：** `typedef logic [63:0] state_t [0:4][0:4];`。
* **常數定義：** 24 個 Round Constants（記得檢查上面「已知問題」提到的 round 18 / round 23）。
* **狀態定義：** 主 FSM (`ST_IDLE`/`ST_PAD`/`ST_ABSORB`/`ST_RUN_ROUND`/`ST_SQUEEZE`/`ST_DONE`)、Theta 專用的 Enum (`THETA_IDLE`/`THETA_CALC_C`/`THETA_CALC_D`/`THETA_UPDATE`)、Chi 專用的 Enum (`CHI_IDLE`/`CHI_CALC`)、Round Scheduler 的 Enum (`SCHED_*`，共 8 態) 與 Squeeze 的 Enum (`SQZ_IDLE`/`SQZ_RUN`/`SQZ_DONE`)。



#### Phase 2: 核心攻堅 (各自展開)

**【Person B：負責 Keccak 多週期序列邏輯】** 這是本次架構改版的核心，務必加上時序控制！

* **`keccak_theta_serial.sv`**
* **I/O 注意：** `input clk`, `input rst_n`, `input start`, `input state_t in_state`, `output state_t out_state`, `output logic done`, `output logic [24:0] lane_active`。
* **注意陷阱：** 內部需要實作一個小型計數器或狀態機。在 Cycle 1~5 時，把算好的 $C[x]$ 存進內部的 DFF 快取中；切勿在算完前就把 `done` 拉高。`lane_active` 只在 `THETA_UPDATE` 階段、且僅對當前那一欄的 5 個 lane 拉高，其餘時間全 0。


* **`keccak_chi_row.sv`**
* **I/O 注意：** 介面同上 (`clk`, `rst_n`, `start`, `done`, `[24:0] lane_active`)。
* **注意陷阱：** 一次只抓取一整個 row (共 320 bits) 進行運算，利用內部 Counter `cnt` 從 0 數到 4，第 5 個 Cycle 結束時拉高 `done`。務必在 `start` 那一拍把整包 `in_state` 凍結進 `frozen_state`，後續運算只讀凍結值，理由見上方 Phase 2 說明（Pi 排列會把其他 row 的資料混進來，不凍結會讀到被前面 row 已經改寫過的髒資料）。



**【Person A：負責記憶體與通道級省電】**

* **`keccak_state_bank.sv` (狀態暫存器)**
* **I/O 注意：** `input [24:0] lane_clk` 接收 25 組獨立 gated clock；`input state_t nxt_state`, `output state_t cur_state`, `input rst_n`。**沒有 `lane_wr_en` 這個埠**，這點請務必更新舊筆記，寫入與否完全由 `lane_clk[i]` 這個 cycle 有沒有 posedge 決定。
* **注意陷阱：** 1600 bits 必須拆成 25 塊 64-bit 的 `generate` block，每塊各自 `always_ff @(posedge lane_clk[LANE_IDX] or negedge rst_n)`，`LANE_IDX = y*COL_NUM + x`。這需要你與 Top 層的 `state_active_mask` 高度連動——`state_active_mask` 算錯，clock gating 就跟著錯。



#### Phase 3: 控制與外殼 (各自展開)

**【Person A：負責心臟與大腦】**

* **`keccak_round_scheduler.sv` (排程器)**
* **I/O 注意：** 需要接收來自 Theta 和 Chi 的 `done` 訊號。
* **注意陷阱：** 這是個 8 態微狀態機。收到 FSM 的 `start_process` 後，流程為：`SCHED_THETA_START` 觸發 `theta_start` -> `SCHED_THETA_WAIT` 等 `theta_done` -> `SCHED_RHO_PI_START`/`SCHED_RHO_PI_WAIT`（各 1 cycle，純轉場，Rho/Pi 本身不需要額外 cycle）-> `SCHED_CHI_START` 觸發 `chi_start` -> `SCHED_CHI_WAIT` 等 `chi_done` -> `round_index + 1`。重複 24 次後發出 `process_done`。每輪固定 20 cycles，24 輪共 480 cycles。


* **`sha3_ctrl_fsm.sv` (主狀態機)**
* **I/O 注意：** `input clk`, `input rst_n`, `input in_valid`, `input process_done`, `input squeeze_done`, `output absorb_en`, `output start_process`, `output squeeze_start`, `output hash_done`。
* **狀態流程：** `ST_IDLE -> ST_PAD -> ST_ABSORB -> ST_RUN_ROUND -> ST_SQUEEZE -> ST_DONE -> ST_IDLE`。`ST_RUN_ROUND` 與 `ST_SQUEEZE` 分別停留到收到 `process_done` 與 `squeeze_done`。
* **注意陷阱：** `absorb_en` 只在 `ST_ABSORB` 拉高；`start_process` 只在進入 `ST_RUN_ROUND` 時拉高一個 cycle；`squeeze_start` 只在進入 `ST_SQUEEZE` 時拉高一個 cycle；`hash_done` 只在 `ST_DONE` 拉高一個 cycle。不可將 `start_process` 直接解碼成整段 `ST_RUN_ROUND` 都為 1，也不可由主 FSM 產生 Formatter 的 `out_valid`。
* **資料責任：** 主 FSM 只輸出控制訊號，不保存 1080-bit message。Top 層需在接受 `in_valid` 時鎖存 `msg_in`/`msg_length`，並將鎖存值送入 Padding 模組。



**【Person B：負責資料處理與外殼】**

* **`sha3_pad_domain.sv`**
* **I/O 注意：** `input [1079:0] msg_in`, `input [7:0] msg_length`, `output [1087:0] padded_block`。
* **注意陷阱：** 維持純組合邏輯，精準計算 `10...01` 對齊即可。


* **`sha3_output_formatter.sv`**
* **I/O 注意：** `input clk`, `input rst_n`, `input start`, `input state_t in_state`, `output logic [31:0] out_data`, `output logic out_valid`, `output logic done`。
* **注意陷阱：** 容易搞錯 Lane 的線性化順序，記得是 `z = x + 5y`，SHA3-256 只需要 `y=0` 那一列的 `x=0~3`，不是隨便挑 4 個 Lane。內部只有 `SQZ_IDLE`/`SQZ_RUN`/`SQZ_DONE` 三個狀態，用 `cnt[2:1]` 選 Lane、`cnt[0]` 選低/高 32-bit，不是四個狀態。另外要注意 64-bit Lane 拆成兩個 32-bit word 時的 endianness，務必和 Python `hashlib.sha3_256` 的 byte order 對齊，否則 Phase 4 比對雜湊值會一直失敗卻抓不到原因。



#### Phase 4: 系統整合

* **`sha3_ultra_low_power_top.sv`**
* 只負責整合接線與輸入保存，不包含 SHA-3 運算。Top 在主 FSM 接受 `in_valid` 時鎖存 `msg_in`/`msg_length`，並將鎖存值送入 Padding；將 `sha3_low_power_gating` 的 `[24:0] gated_clk` 一對一接至 `keccak_state_bank` 的 `[24:0] lane_clk`；特別注意 `start` 和 `done` 的交握網路是否有正確串接，以及 `state_active_mask`（優先序：`accept_msg` > `absorb_en` > `theta_lane_active` > `chi_lane_active`，且 lane(0,0) 在 `chi_lane_active` 分支內特別改接 `iota_state`）有沒有正確反映當前 Theta/Chi 算到的進度。當 `sleep_en[i] == 1` 時，對應 Lane 的時脈必須停止；系統裡沒有 `lane_wr_en`，不要再找這個訊號。

---

### 附錄：與設計指南的符合度檢查

對照 `ultra_low_power_sha3_architecture.md` 的 Candidate 3 定義，以及 `sha3LP.md` 第 7~11 節提出的具體機制（第 1~6、12~14 節是投稿/研究故事包裝，不是 RTL 規格，不列入本檢查）。

| 指南要求 | 出處 | 現況 | 結論 |
| --- | --- | --- | --- |
| 25 × 64-bit gated lane 儲存 | arch.md Candidate 3；sha3LP.md §10 機制1 | `keccak_state_bank.sv` 25 個獨立 `generate` block，逐 lane 接收各自的 `lane_clk` | ✅ 符合 |
| Lane 級 clock gating（只有被寫的 lane 才跳動） | arch.md §8.1 | `state_active_mask` → `sleep_en` → `sha3_low_power_gating` → 25 組 `gated_clk` | ✅ 符合 |
| θ：先算 column parity 再統一更新 | arch.md §7.1（1. 算 C[0..4] 2. 算 D[0..4] 3. 逐欄更新） | `THETA_CALC_C`(5) → `THETA_CALC_D`(1) → `THETA_UPDATE`(5) | ✅ 完全對應建議排程 |
| ρ/π 做成固定 wiring、不用 barrel shifter | arch.md §7.2 | `keccak_rho_pi_wire.sv` 用常數迴圈展開的固定旋轉，0 cycle | ✅ 符合 |
| χ 做 row-serial | arch.md §7.3；sha3LP.md §10 機制5 | `keccak_chi_row.sv` 逐 row（5 cycle），且用 `frozen_state` 鎖存輸入避免跨 row 髒資料 | ✅ 符合，且比單純 row-serial 多做了必要的 snapshot 保護 |
| θ parity cache（C/D 暫存） | sha3LP.md §10 機制3 | `keccak_theta_serial.sv` 內的 `C[]`/`D[]` 暫存器 | ✅ 符合 |
| Rho/Pi 邏輯 remap 而非實體搬線 | sha3LP.md §10 機制4 | 固定接線本身就是最徹底的「remap」，甚至不需要額外控制器追蹤 | ✅ 符合 |
| Operand isolation（避免無效 XOR tree 切換） | arch.md §8.2 | 每個 cycle 的 θ/χ 運算只作用在當前欄/row，其餘 lane 走 `out_state = in_state` 直通；沒有針對 `C[]`/`D[]` 輸入額外加隔離閂鎖 | 🟡 精神上符合，未做到最嚴格的顯式 operand isolation，可列為後續優化項 |
| Cycles / permutation：數百到上千 | arch.md Candidate 3 表格 | 實測每輪 20 cycles，24 輪 = **480 cycles**（比 arch.md 範例算的 576 cycles 更省） | ✅ 落在建議區間內，且效率略優於範例值 |
| 雙模式 folded datapath（320-bit row / 64-bit lane 可切換） | sha3LP.md §10 機制2 | 目前固定跑 320-bit（row/column）粒度，沒有獨立的純 64-bit 單 lane 模式，也沒有 mode 暫存器 | ⚪ 未實作 —— 這屬於 sha3LP.md 提出的 ASAKE 研究延伸方案，不是 Candidate 3 的基本要求，超出本次分工範圍 |
| Active/retention/sleep 電源模式 | sha3LP.md §10 機制6 | 只有「持續 active + per-lane clock gating」，沒有獨立的省電狀態機 | ⚪ 未實作 —— 同上，屬於研究延伸方案，非本次分工範圍 |
| 效能/切換活動計數器（`performance_counter_block`） | sha3LP.md §11 | 已確定不做 `sha3_perf_counter.sv`（見上方 Phase 說明） | ⚪ 依團隊決議明確不做 |

**結論：** 就 `分工說明.md` 承諾的 Candidate 3 基本款而言，目前 RTL 在儲存粒度、clock gating、θ/χ 排程與 ρ/π wiring 上都對得起設計指南；唯一真正「未實作」的兩項（雙模式折疊、電源模式）屬於 `sha3LP.md` 為了論文貢獻另外加碼的 ASAKE 研究方向，不在 Candidate 3 或本分工文件的承諾範圍內，不需要為此補工。真正該記住的待辦事項是前面提到的 Round Constant 修正，以及排程器裡 Rho/Pi 那 2 個純轉場 cycle（可作為未來壓 cycle 數的優化點）。
